global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: blackrouter
    metrics_path: /metrics
    static_configs:
      - targets:
          - blackrouter:${BLACKROUTER_PORT:-20129}

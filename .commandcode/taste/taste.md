# Taste (Continuously Learned by [CommandCode][cmd])

[cmd]: https://commandcode.ai/

# rust
- For the blackrouter project: use `cargo run` directly for testing instead of Docker build. Confidence: 0.80
